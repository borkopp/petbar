/**
 * Privacy utility for user information
 * Returns a generic placeholder instead of displaying actual user information
 * Used to completely hide usernames and emails for privacy reasons
 */
export function obfuscateUsername(): string {
  return "Корисник";
} 